# دليل البدء السريع

## المشروع جاهز للنشر! 🎉

تم إعداد المشروع بالكامل ورفعه على GitHub. الآن يمكنك نشره على استضافة مجانية في دقائق.

## رابط المستودع
https://github.com/bdrnjom007/schengen-visa-extractor

## خطوات النشر السريعة على Render

### 1. إنشاء حساب
- اذهب إلى: https://render.com
- سجّل حساب جديد (استخدم GitHub للتسجيل السريع)

### 2. إنشاء قاعدة بيانات
- من Dashboard، اضغط "New +" → "PostgreSQL"
- Name: `schengen-visa-db`
- Database: `schengen_visa`
- User: `schengen_user`
- Region: اختر الأقرب لك
- Instance Type: **Free**
- اضغط "Create Database"
- **احفظ "Internal Database URL"** (ستحتاجه لاحقاً)

### 3. إنشاء Web Service
- من Dashboard، اضغط "New +" → "Web Service"
- اختر "Build and deploy from a Git repository"
- اضغط "Connect GitHub" وأذن لـ Render
- اختر repository: `schengen-visa-extractor`
- اضغط "Connect"

### 4. إعدادات Web Service

**Name**: `schengen-visa-api`

**Region**: نفس منطقة قاعدة البيانات

**Branch**: `master`

**Root Directory**: اتركه فارغاً

**Runtime**: `Node`

**Build Command**:
```
cd server && npm install && npm run build
```

**Start Command**:
```
cd server && npm start
```

**Instance Type**: **Free**

### 5. إضافة متغيرات البيئة

اضغط "Advanced" ثم أضف المتغيرات التالية:

```
NODE_ENV=production
PORT=3000
DATABASE_URL=[الصق Internal Database URL من الخطوة 2]
OPENAI_API_KEY=AIzaSyCIbOkfKF_ZQ84TLBQL8Sgprk_F1FPHces
```

### 6. النشر
- اضغط "Create Web Service"
- انتظر 5-10 دقائق حتى ينتهي البناء والنشر
- ستحصل على رابط مثل: `https://schengen-visa-api.onrender.com`

### 7. إنشاء الجداول في قاعدة البيانات
بعد نجاح النشر:
- اذهب إلى Web Service
- اضغط "Shell" من القائمة الجانبية
- نفّذ الأمر:
```bash
cd server && npm run db:push
```

## التحقق من النشر

زر الرابط:
```
https://schengen-visa-api.onrender.com/health
```

يجب أن ترى:
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

## نشر Frontend (اختياري)

إذا أردت نشر واجهة المستخدم أيضاً:

### على Vercel (موصى به)
1. اذهب إلى https://vercel.com
2. سجّل حساب جديد
3. اضغط "Add New" → "Project"
4. اختر `schengen-visa-extractor`
5. Root Directory: `client`
6. Framework Preset: `Vite`
7. Build Command: `npm run build`
8. Output Directory: `dist`
9. Environment Variables:
   ```
   VITE_API_URL=https://schengen-visa-api.onrender.com/trpc
   ```
10. اضغط "Deploy"

## ملاحظات مهمة

⚠️ **الخطة المجانية على Render**:
- الخدمة تتوقف بعد 15 دقيقة من عدم النشاط
- تستيقظ تلقائياً عند أول طلب (قد يستغرق 30 ثانية)
- 750 ساعة مجانية شهرياً

⚠️ **قاعدة البيانات المجانية**:
- تُحذف بعد 90 يوم من عدم النشاط
- حجم محدود: 512 MB

## الدعم

إذا واجهت أي مشكلة:
1. تحقق من Logs في Render Dashboard
2. تأكد من صحة متغيرات البيئة
3. تأكد من تشغيل `db:push` بنجاح
4. راجع ملف DEPLOYMENT.md للتفاصيل الكاملة

## مفتاح API

المفتاح المستخدم حالياً:
```
AIzaSyCIbOkfKF_ZQ84TLBQL8Sgprk_F1FPHces
```

⚠️ **تحذير**: هذا مفتاح Gemini API. تأكد من عدم مشاركته علناً وقم بتفعيل القيود عليه من Google Cloud Console.

## الخطوات التالية

بعد النشر الناجح:
1. اختبر رفع الصور واستخراج البيانات
2. راجع الأمان والصلاحيات
3. أضف نظام تسجيل الدخول
4. قم بتخصيص التصميم حسب احتياجاتك

---

**تم إعداد المشروع بواسطة Manus AI** 🤖
